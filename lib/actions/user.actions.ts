"use server";

export const signIn = async () => {
  try {
    // mutation database action
  } catch (error) {
    console.log("Error useraction signIN", error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // mutation database action
  } catch (error) {
    console.log("Error useraction signIN", error);
  }
};
